# BusyLight Presence runtime dependencies.
#
# Core helper relies only on CPython stdlib (winreg, urllib,
# http.cookiejar, configparser). The optional tray-app shell adds
# pystray + Pillow so users get a system-tray icon and a settings
# window instead of a console. Both are pure-Python wheels on PyPI.
pystray>=0.19
Pillow>=10.0
